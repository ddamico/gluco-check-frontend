import { NightscoutValidationClient } from "./NightscoutValidationClient";

describe("NightscoutValidationClient", () => {
  // fetches from endpoint
  // expect url to be the configured option
  // expect to call fetch, json
  // expect to throw if fetch throws
  // expect to throw if response invalid
});
